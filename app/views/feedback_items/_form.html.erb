<%= form_for(@feedback_item) do |f| %>
  <% if @feedback_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback_item.errors.count, "error") %> prohibited this feedback_item from being saved:</h2>

      <ul>
      <% @feedback_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :topic_id %><br />
    <%= f.number_field :topic_id %>
  </div>
  <div class="field">
    <%= f.label :rank %><br />
    <%= f.number_field :rank %>
  </div>
  <div class="field">
    <%= f.label :percentage %><br />
    <%= f.number_field :percentage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
